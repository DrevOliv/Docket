# Used by docker-compose.example.yml — copy to `.env` and edit.

# Absolute path on the HOST to the folder that contains your docker-compose
# project subfolders. Docklet scans this and shows each subfolder with a
# compose file as a "stack".
STACKS_DIR=/home/youruser/docker-stacks
